For the files too large to upload to github (like the .record files and
trained model, they are instead cultivated in the .ipynb file)

# Mask Object Detection

This program trains a neural network to 
